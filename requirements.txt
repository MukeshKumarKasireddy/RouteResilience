# Core ML / segmentation stack (Suryakant - AI/ML Lead component)
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
tqdm>=4.66.0

# Testing
pytest>=7.4.0

# Notebook support
jupyter>=1.0.0

# Optional: GeoTIFF support for image loading (best-effort only in this
# component; full CRS/geospatial handling belongs to the downstream
# network-reconstruction pipeline).
rasterio>=1.3.0; platform_system != "Windows"
